{
  "name": "@spiritledsoftware/elisha",
  "version": "0.4.0",
  "description": "An OpenCode plugin for advanced AI-driven software development assistance.",
  "author": "Spirit-Led Software LLC <social@spiritledsoftware.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/spiritledsoftware/elisha.git"
  },
  "keywords": [
    "opencode",
    "plugin",
    "ai",
    "agent",
    "assistant",
    "vibe-coding"
  ],
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "src",
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "bun build --target=bun --minify --splitting --sourcemap=linked --outdir=dist src/index.ts",
    "build:watch": "bun build --target=bun --sourcemap=linked --outdir=dist --watch src/index.ts",
    "format": "biome format --write",
    "format:check": "biome format",
    "lint": "biome lint",
    "lint:fix": "biome lint --write",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "release": "bun run build && changeset publish",
    "prepare": "husky"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.1.34",
    "@opencode-ai/sdk": "^1.1.34",
    "defu": "^6.1.4",
    "nanoid": "^5.1.6",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@changesets/cli": "^2.29.8",
    "@types/bun": "^1.3.6",
    "husky": "^9.1.7",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "zod": "^4.3.6"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
